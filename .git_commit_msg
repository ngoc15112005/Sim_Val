UI polish: bracket connectors, final standings podium, header redesign

Backend (bracket_service.py):
- get_final_standings(): compute full standings sorted by rank
  - Status labels: Vo dich / A runner-up / Hang ba / Hang tu / Top 5-8 / #9+
  - Status classes: champion / runner-up / third / fourth / ranked / eliminated
  - W-L stats aggregated from all matches (per club)
- Rank 1 always gets Vo dich regardless of tournament type

Frontend (routes.py):
- Pass final_standings to template (only when status=finished)

Template (tournament.html):
- Breadcrumb: Home / {tour_name}
- Header 2-row layout:
  - Row 1: tour-type badge + title + status badge + action buttons
  - Row 2: info chips (team count, format details)
- Final Standings card with 2 parts:
  - Podium top 3 (2nd, 1st, 3rd order) - champion card with gold border
  - Full standings table with rank medals (gold/silver/bronze)

CSS (~190 lines):
- Bracket connector lines (::before, ::after pseudo-elements)
  - Horizontal line from right edge of each match
  - Vertical line between consecutive matches
  - Use CSS vars for color
- Breadcrumb: subtle divider style
- Tour header: 2-row flex, action buttons (primary/secondary/ghost)
- Status badges: large colored badges
- Tour type badges: color-coded (major=blue, master=purple, champion=orange)
- Info chips: pill style for format details
- Podium: 3-col grid (2nd, 1st, 3rd order)
  - Champion card: gold border + glow effect
  - Rank colors: gold/silver/bronze for top 3
  - Podium bar (color bar under card)
- Full standings table: rank medals (circle badges), status pills
- Mobile responsive (podium stacks vertically on small screens)

Fixes:
- Jinja2 set variable scoping in for loop - use namespace
- Major rank 1 now correctly shows Vo dich (was previously #1)
- All format details extracted to info chips
